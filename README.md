# SQL-lite3-Analysis-Task2
Redesigned the students table using proper data types and integrity constraints like PRIMARY KEY, NOT NULL, and UNIQUE to ensure data quality. Tested constraint violations using invalid inserts. Modified the schema with ALTER TABLE to add and rename columns, safely dropped a column by recreating the table, and closed the database safely!!
